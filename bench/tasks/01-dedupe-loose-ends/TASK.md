# Task: dedupe loose ends

Read `loose-ends.md` in this directory. Under its `## Open` heading are
`- [ ] text` lines, some exact duplicates. Write `output.md` containing the
deduplicated list: one `- [ ] text` line per unique item, in the order each
item first appeared, no header, no blank lines, no other text.
