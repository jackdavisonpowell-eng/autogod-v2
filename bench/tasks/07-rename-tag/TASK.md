# Task: rename a tag

Across every `.md` file in this directory, rename the tag `old-tag` to
`new-tag` everywhere it appears: as a YAML frontmatter list item
(`- old-tag`) and as an inline `#old-tag` reference in body text. Do not
touch any other tag (e.g. leave `keep-tag`, `misc` untouched). Edit the
files in place.
