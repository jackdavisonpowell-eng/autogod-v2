# Task: CSV top 3

Write `run.sh` (any language it invokes) that, run with no arguments in
this directory, reads `data.csv` (header `name,score`) and prints to
stdout the 3 rows with the highest numeric `score`, descending by score,
ties broken by `name` ascending, one per line as `name,score`. No other
stdout output.
