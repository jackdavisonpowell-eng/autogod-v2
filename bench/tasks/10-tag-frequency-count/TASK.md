# Task: tag frequency count

Each `.md` file in this directory has YAML frontmatter with a `tags:` list
(items as `  - tagname` lines). Count how many notes each tag appears in
across all files. Write `freq.md`: one line per tag, format `tag: count`,
sorted by count descending, ties broken alphabetically by tag ascending.
