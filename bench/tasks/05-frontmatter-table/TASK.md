# Task: frontmatter table

Each `.md` file in this directory has YAML frontmatter between `---` lines
with a `name:` and `type:` field. Read all 8 note files and write
`table.md`: a markdown table with header `| name | type |` and a separator
row, then one row per note, `| name | type |`, sorted by `name` ascending.
