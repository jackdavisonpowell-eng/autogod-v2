# Task: add missing Hub line

For every `.md` file in this directory that does NOT contain a line
starting with `Hub:`, append a new line `Hub: [[Unsorted]]` to the end of
the file (add a trailing newline first if needed). Do not modify files that
already contain a `Hub:` line in any way.
