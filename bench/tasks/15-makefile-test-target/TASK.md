# Task: Makefile test target

`calc.py` and `test_calc.py` (a Python `unittest` suite) already exist here.
Write a `Makefile` with a `test` target that runs the test suite and exits
non-zero if any test fails. Write `run.sh` that just runs `make test`.
