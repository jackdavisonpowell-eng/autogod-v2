# Task: summarise the journal week

Read `journal.md`, a week of daily journal entries (one day per section).
Write `summary.md`: exactly 5 bullet points (`- ...`), each summarising
something notable from the week, each ending with a citation to the exact
source line in the form `(line N)` where N is the 1-indexed line number in
`journal.md` that supports the bullet.
